'use strict';

/**
 * companies-bookmark router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::companies-bookmark.companies-bookmark');
