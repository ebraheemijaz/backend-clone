'use strict';

/**
 * domain-bookmark controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::domain-bookmark.domain-bookmark');
